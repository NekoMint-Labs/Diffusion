"""Search sources and readers.

Transports are imported by :mod:`diffusion_discovery.providers.registry` rather
than here, so importing the package does not pull in HTTP dependencies or read
configuration.
"""
