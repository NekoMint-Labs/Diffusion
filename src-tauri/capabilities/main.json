{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "User-selected file access, explicit HTTP(S) opening, OS-backed credential storage, and the bundled discovery engine. No shell plugin and no unrestricted filesystem scope: the discovery engine is a resource whose path the native layer resolves itself, so no webview-supplied command can decide what runs.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:default",
    "fs:allow-read-file",
    "fs:allow-write-file",
    "fs:allow-stat",
    {
      "identifier": "opener:allow-open-url",
      "allow": [
        {
          "url": "https://*"
        },
        {
          "url": "http://*"
        }
      ]
    }
  ]
}
